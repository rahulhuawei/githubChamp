name: My First Workflow
on: push
jobs:
    first_job:
        runs-on: ubutu-latest
        steps:
            - name: welcome message
              run: echo "My first Github Action Job"
            
            - name: List files
              run: ls
              
            - name: Read files
              run: cat README.md